import 'RelatedProperties.dart';

class RelatedSection {
 final String title;
 final String subtitle;
 final List<RelatedProperties> relatedProperties;

 RelatedSection({
  required this.title, required this.subtitle,required this.relatedProperties
 });
}